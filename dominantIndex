class Solution(object):
    def dominantIndex(self, nums):
        mx = max(nums)
        m = nums.index(mx)
        for i in range(len(nums)):
            if i != m and 2 * nums[i] > mx:
                return -1
        return m
